mock-connector file 1 content

Here's a simple Java code to reverse a string without using any built-in string methods:\\n\\n```java\\npublic class ReverseString {\\n    public static void main(String[] args) {\\n        String input = \\\"Hello, World!\\\";\\n        System.out.println(\\\"Original String: \\\" + input);\\n\\n        // Reversing the string\\n        String reversed = reverseString(input);\\n        System.out.println(\\\"Reversed String: \\\" + reversed);\\n    }\\n\\n    public static String reverseString(String str) {\\n        char[] charArray = new char[str.length()];\\n\\n        // Copy characters in reverse order to charArray\\n        for (int i = 0; i < str.length(); i++) {\\n            charArray[i] = str.charAt(str.length() - 1 - i);\\n        }\\n\\n        // Build the reversed string from the character array\\n        StringBuilder reversedStr = new StringBuilder();\\n        for (char c : charArray) {\\n            reversedStr.append(c);\\n        }\\n\\n        return reversedStr.toString();\\n    }\\n}\\n```\\n\\n### Explanation:\\n\\n1. **Input String**: The string to reverse is given as `input`.\\n2. **Reversing**: The program uses a `char` array to store characters of the string in reverse order, one by one.\\n3. **Result**: It then builds the reversed string using a `StringBuilder`, which efficiently appends characters.\\n\\nLet me know if you need more details!\\n\\n